import { style } from "@vanilla-extract/css";

export const listReset = style({
  margin: 0,
  padding: 0,
  listStyle: "none",
  minWidth: 0,
  minHeight: 0,
});

export const listItem = style({
  minWidth: 0,
  minHeight: 0,
});
