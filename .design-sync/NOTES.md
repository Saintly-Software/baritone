# design-sync notes — @saintly-software/baritone

Source shape: **storybook**. Global: `window.SaintlySoftwareBaritone`. Build: `pnpm build` → `dist/index.js` (+ `dist/styles.css`, vanilla-extract). DS's own source repo, so converter runs with `--entry ./dist/index.js` (no `node_modules/<pkg>`).

## Decisions

- [GENERAL] Theme is applied by the `.storybook/preview.tsx` decorator via vanilla-extract classes `lightTheme`/`darkTheme` (from `src/theme`), mirrored onto `<body>` so base-ui portaled popups (Modal, Drawer, Popover, Menu, Combobox, Select, Tooltip) resolve theme CSS vars. Decorator auto-bundles as `preview-decorators.js` — previews inherit it. Runtime alternative provider is the exported `BaritoneTheme` component (inline CSS custom props).
- `Internal/*` stories (InternalButton, InternalCheckbox, InternalGenericButtonAnchor, InternalSpinner, InternalSwitch, InternalTooltip) are **not package exports** — excluded via `titleMap {<tail>: null}`. They are implementation building blocks, not design-system surface.
- `Interaction Tests/*` stories (`*.interaction.stories.tsx`, play-function tests) share the tail name of the real component, so Storybook merges them in as extra sibling stories of Button/Card/Combobox/InaccessibleTooltip/Link/Popover/ScrollArea/Select/Text/Tooltip. `titleMap` cannot exclude them (tail collides with the real export). Kept for now — several (Popover/Tooltip side variants, ScrollArea orientations) are legitimate visual references; they render identically on both panels. Revisit any that render as static test-noise during grading via `cfg.overrides.<Name>.skip`.

## Build/render fixes (self-heal loop)

- [GENERAL] **VE runtime throw "Styles were unable to be assigned to a file"** on every preview. Root cause: the lib build (`.config/vite.config.ts`) externalizes `@vanilla-extract/css` as build-time-only, and the auto-bundled `.storybook/preview.tsx` decorator imports the theme from `../src/theme` **source** `.css.ts` — esbuild can't run the VE compiler, so `createTheme()` runs at runtime and throws. Fix: replaced the decorator with `cfg.provider = {component: "PreviewRoot"}`, a wrapper (`.design-sync/preview-provider.tsx`, exposed via `cfg.extraEntries`) that sources the theme from the COMPILED `../dist` and mirrors the theme CSS vars onto `document.body` so base-ui portaled overlays stay themed (see [[portaled-surfaces-unstyled-in-storybook]]).
- **BaritoneTheme story** imported `{ buildDefaultTokens, vars }` from `../../theme` (the theme index barrel) → resolves to `src/theme/index.ts`, which re-exports `defaultTheme.css.ts` (the throwing `createTheme`). Not an exported _component_, so the default import policy bundled it from source and threw. Fix: `cfg.storyImports.shim: ["src/theme/index"]` forces the barrel to the compiled global (`buildDefaultTokens`/`vars` are bundle exports). **Scope matters:** shim only `src/theme/index`, NOT `src/theme` — Switch/Text import `LABEL_POSITIONS`/`TEXT_SIZES`/`TEXT_WEIGHTS` from `../../theme/constants`, and those are NOT re-exported by the package (not on the global), so a broad shim makes them `undefined` (`.map` of undefined). `constants.ts` is pure — safe to bundle from source.

## Grid-overflow / card presentation (presentation-only overrides)

- `cardMode: "column"` (wide layouts): Badge, Link, ToggleGroup, Box, Flex, ScrollArea, BaritoneTheme.
- `cardMode: "single"` (portaled/escape overlays) + a `primaryStory`: Meter, Combobox, Select, ConfirmationModal, Drawer, Menu, Modal, Popover. primaryStory picks are provisional — refine during grading if the chosen story renders poorly as a single card.

## Overlay / interaction-story handling (batch 1)

- [GENERAL] **Play-driven stories don't render their post-interaction state in the preview.** The compare harness runs storybook's `play` (which opens a menu/tooltip/popover or picks an option), but the compiled static preview cannot execute `play` — so those stories show the _initial_ (closed/unselected) state and mismatch storybook. Rule: skip the play-driven interaction stories via `cfg.overrides.<Name>.skip`; keep the base `.stories.tsx` stories that render statically (they use `defaultOpen`/controlled state).
  - Skipped: Menu (`surfaces-menu--{opens-and-highlights-item,keep-open-stays-open,side-top,side-bottom,side-left,side-right}`), Popover (`interaction-tests-popover--{kitchen-sink,side-top,side-right,side-bottom,side-left}`), Select (`interaction-tests-select--{single-select-picks-option,multi-select-hover-selected-option}`), Tooltip (`interaction-tests-tooltip--{opens-on-focus,long-content,side-top,side-right,side-bottom,side-left}`).
  - Statically-open base stories that MATCH as-is: Modal (all), Drawer (all), ConfirmationModal (all), Popover KitchenSink (`defaultOpen`), Menu KitchenSink (`defaultOpen`), Combobox/Select form-field stories.
- **Tooltip** has NO static-open story — even its base `Basic` opens via a `play` hover. Owned preview `.design-sync/previews/Tooltip.tsx` forces `defaultOpen` on Basic so the card shows the open tooltip and matches storybook's played-open reference. (Menu/Popover didn't need this — their base KitchenSink already uses `defaultOpen`.)
- InaccessibleTooltip: both stories render statically (Basic = underlined text; OpensOnHover trigger closed on both sides) → match, no skip.

## More interaction-story skips (batches 3–4)

- **Card** (`interaction-tests-card--{clickable-activates,collapsible-toggles}`): play increments/toggles state (storybook shows "Pressed 1 time"; preview initial "0 times") → skipped. Base stories (Kitchen Sink, Clickable, Linkable, Selected, Intents And Saliencies) render statically → match.
- **ScrollArea** (`interaction-tests-scrollarea--{vertical,horizontal,both}`): these render identically to the base orientation stories (scroll position doesn't diverge), so they MATCH — but they're exact visual duplicates of Vertical (default)/Horizontal/Both, so skipped purely to de-dupe the column card, not to fix a mismatch.
- Everything else in batches 2–4 (all form controls, Box/Flex/Grid, Badge/ButtonGroup/Chip/ChipList/Link/Lockup/Notice/ToggleButton, Accordion/CardList) matched as-is with no skips — statically-rendered components.

## Re-sync 2026-08-11 (v1.0.0-alpha.4) — +8 components, typography breaking changes

Diff vs prior anchor: 20 unchanged, 25 changed, 8 added (Divider, Field, Lightbox, LinkProvider, List, Overflow, SegmentedBar, Table). Everything graded `match` (no `close`, no owned-preview fixes beyond Field).

- **Field** — its story imports VE recipe SOURCE (`../../styles/recipes/{formControl,focusRing}.css`) AND calls `controlClassFor()` at MODULE TOP LEVEL → esbuild can't run the VE compiler → runtime throw "Styles were unable to be assigned to a file" on import → root-empty (`✗[RENDER]`, the one validate blocker). Fix: owned `.design-sync/previews/Field.tsx` re-implements the stories importing only `@saintly-software/baritone`, reproducing the control skin inline from `vars.form.color[state].{background,border}` + `vars.form.borderRadius`/`vars.space[3]`/`vars.text.size.md.fontSize`/etc. (the same tokens `formControlRecipe` reads). Field itself is the real shipped component; only the raw input's skin is rebuilt. `grep` confirmed ONLY Field's story imports recipe sources → NOT [GENERAL].
- **Lightbox** — `cardMode:single, primaryStory:"Open"`. Skipped 2 play-driven interaction tests (`interaction-tests-lightbox--{opens-from-trigger,closes-from-close-button}`). `Open` (defaultOpen) renders the picsum remote image on BOTH panels — no `[ASSETS_BLOCKED]`.
- **Overflow** — skipped 3 play-driven interaction tests (`interaction-tests-overflow--{horizontal,horizontal-page,vertical}`): they scroll/click, so storybook shows post-play position vs preview's initial. 5 base stories match.
- **LinkProvider** — provider that renders its Links visibly (not empty). Skipped 1 play-driven interaction test (`interaction-tests-linkprovider--routes-internal-links`). Base `With Mock Router` matches.
- **Select** — appended `interaction-tests-select--grouped-selection` to its existing skip list (new play-driven sibling this release).
- **Button / Text / Table** — `cardMode:"column"` (grid-overflow `wide`: Button `Width`, Text `CustomSizes`, Table `Basic/CustomCells`).
- **InternalChip** — new `Internal/*` export → `titleMap: null` (excluded like its siblings).
- **Toast** (NEW #96) — DEFERRED by user choice. Hook/portal API: exports `useToast` + `ToastViewport`, NO `Toast` export; stories fire toasts imperatively from buttons (static render = buttons only). Excluded via `titleMap {"Toast": null}`. To sync later: `titleMap {"Toast":"ToastViewport"}` + owned preview firing a toast on mount (`cardMode:single`) — renders the real Notice-skinned toast but won't pixel-match the buttons-only storybook reference (grade on the component render, note the gating).
- **DataTable** (TanStack v9) — DEFERRED by user choice. Lives on the `./datatable` SUBPATH (`dist/datatable.js`), NOT `dist/index.js`, so absent from `window.SaintlySoftwareBaritone`; never synced. Excluded via `titleMap {"DataTable": null}`. To sync later: `cfg.extraEntries: ["./dist/datatable.js"]` + owned fixture previews (column helpers) + `cardMode:column`.
- **Typography (Text/Heading, #93/#95 breaking)** — dynamic consumer-defined `size`/`weight`/`letterSpacing`/`lineHeight`. All Text stories match incl. `Custom Sizes` (hero/figure), `Custom Weights` (hairline/black), custom letter-spacing/line-height; Heading (Basic/Levels) match. `size` is now a widened type alias in `.d.ts` (not an inline union) — the built-in ramp `xs..9xl` still valid (`TEXT_SIZES`).
- [GENERAL] **Compare-capture framing clip** — the `PreviewRoot` 2rem padding + fixed capture viewport can crop the LAST row / WIDEST column of tall/wide stories on the preview side (seen: Text `Sizes` superbold col, FileList `Kitchen Sink` long-names section, Select `States` loading row). Benign framing artifact, NOT a mismatch — confirm the content is in the story's render before grading `mismatch`. The product card is unaffected (column/single modes give full width).
- [GENERAL, confirmed working] `PreviewRoot` body-mirrored CSS vars keep base-ui portaled overlays (Drawer/Menu/Tooltip/etc.) themed — re-verified this run, no theming break.

## Re-sync risks

- **Field owned preview** (`.design-sync/previews/Field.tsx`) reproduces `formControlRecipe` inline from `vars.form.color[state].{background,border}`, `vars.form.borderRadius`, `vars.space[3]`, `vars.text.size.md.fontSize`, `vars.borderWidth.thin`, `vars.font.sans`, `vars.text.color.neutral.high`, and renders `Field.Control` (compound). If the `vars.form` token shape changes or `Field.Control` is dropped, the card silently un-styles (inline `undefined` won't throw). Re-verify Field renders styled on re-sync.
- `.design-sync/preview-provider.tsx` imports `createInlineTheme`/`vars`/`buildDefaultTokens`/`BaritoneTheme` from `../dist/index.js` (all current bundle exports). If any export name changes the provider breaks. `pnpm build` must run before the converter so `dist/` exists.
- `cfg.storyImports.shim: ["src/theme/index"]` assumes everything BaritoneTheme's story imports from the theme barrel is a bundle export.
- **New `*.interaction.stories.tsx` siblings** merge under the real component's tail name; play-driven ones need `cfg.overrides.<Name>.skip` (handled: Lightbox/Overflow/LinkProvider/Select/Menu/Popover/Tooltip/ScrollArea/Card). A newly-added one surfaces as a compare mismatch showing initial-vs-post-play state.
- **conventions.md** predates the dynamic-typography feature (#93/#95): it documents the built-in `size` ramp (still accurate) but doesn't advertise consumer-defined size/weight/letterSpacing/lineHeight. Optional enhancement, not drift — all enumerated names still verify against the build.
- **Toast + DataTable** remain unsynced by user choice this run — revisit if they should join the design surface (recipes above).
- primaryStory picks for single-mode overlays chosen for representativeness, not verified per-pixel as the card's sole story.
- Story caps (Button 14 / Chip 18 / Combobox 20 / Badge 9 / Text graded 12 via `--max-stories`, etc.) leave the tail beyond 6 verified-by-upload, not individually graded.
