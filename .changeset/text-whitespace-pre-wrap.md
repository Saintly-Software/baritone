---
"@saintly-software/baritone": minor
---

Add `pre-wrap` to the `whiteSpace` atom, so `Text` (and `Heading`, which shares
the atom) can preserve newlines and runs of spaces in its content while still
wrapping at the container edge — useful for user-authored copy, log lines, and
model output.
